namespace DungeonVM.Core.Enums;

/// <summary>총 6종의 무기. 칼/방패/단검은 근접(전열), 활/지팡이/성서는 원거리(후열)로 자동 배치된다.</summary>
public enum WeaponType
{
    Sword,
    Shield,
    Dagger,
    Bow,
    Staff,
    Bible,
}
